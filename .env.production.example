# ═══════════════════════════════════════════════════════════════
# Bonito Production Environment Variables
# ═══════════════════════════════════════════════════════════════
# Copy to .env.production and fill in real values.
# For Railway: set these in the Railway dashboard per-service.
# For Vercel: set in Vercel project settings.

# ── Environment ────────────────────────────────────────────────
ENVIRONMENT=production

# ── Database (Railway Postgres addon provides this) ────────────
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/bonito
# Note: Railway sets DATABASE_URL automatically. You may need to
# replace "postgresql://" with "postgresql+asyncpg://" in config.

# ── Redis (Railway Redis addon provides this) ──────────────────
REDIS_URL=redis://default:password@host:6379

# ── Vault ──────────────────────────────────────────────────────
VAULT_ADDR=http://vault-service.railway.internal:8200
VAULT_TOKEN=your-vault-root-token
# If Vault is unavailable, the backend falls back to env vars below.

# ── Secrets (fallback if Vault is unavailable) ─────────────────
SECRET_KEY=generate-with-openssl-rand-hex-32
ENCRYPTION_KEY=generate-with-openssl-rand-hex-32
GROQ_API_KEY=your-groq-api-key
NOTION_API_KEY=your-notion-api-key
NOTION_PAGE_ID=your-notion-page-id
NOTION_CHANGELOG_ID=your-notion-changelog-id

# ── CORS ───────────────────────────────────────────────────────
CORS_ORIGINS=https://bonito.vercel.app,https://bonito.ai

# ── Frontend (Vercel) ─────────────────────────────────────────
NEXT_PUBLIC_API_URL=https://bonito-api.up.railway.app
NEXTAUTH_URL=https://bonito.vercel.app
NEXTAUTH_SECRET=generate-with-openssl-rand-hex-32

# ── Database Pool ──────────────────────────────────────────────
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
